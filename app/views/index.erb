<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>All Recipes</title>
  </head>
  <body>
  <% @recipes.each do |recipe| %>
    <a href="/recipes/<%= recipe.id %>"><%= recipe.name %></a>
    <br>
  <% end %>
  </body>
</html>